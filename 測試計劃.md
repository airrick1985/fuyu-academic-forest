# ✅ 富宇學森 EXE 完整測試計劃

## 📋 打包後驗證清單

### 第 1 階段: 基本驗證

- [ ] **dist/ 文件夾存在**
  ```bash
  ls dist/
  # 應該看到: out, win-unpacked, *.exe
  ```

- [ ] **EXE 文件大小合理**
  ```bash
  ls -lh dist/*.exe
  # 應該 > 150MB（包含 Electron 運行時）
  ```

- [ ] **資源清單已生成**
  ```bash
  ls -la assets-manifest.json
  # 應該看到文件大小 > 10KB
  ```

---

## 🚀 第 2 階段: 應用啟動測試

### 測試 2.1: 開發模式啟動

```bash
npm run electron-dev
```

**檢查點：**
- [ ] 命令行顯示 "✓ Local server running at http://localhost:3456"
- [ ] Electron 窗口自動打開（5-10 秒內）
- [ ] 窗口標題顯示「富宇學森」
- [ ] 首頁內容正常顯示（非白屏）
- [ ] F12 控制台無紅色錯誤

**故障排除：**
如果白屏，按 F12 檢查：
```javascript
// 在控制台執行
console.log('URL:', window.location.href)
document.querySelectorAll('img').forEach(img => {
  if (!img.complete || !img.naturalWidth) {
    console.warn('❌ 圖片加載失敗:', img.src)
  }
})
```

### 測試 2.2: EXE 直接啟動

雙擊或執行：
```bash
./dist/富宇學森-1.0.0-portable.exe
```

**檢查點：**
- [ ] EXE 能直接運行（無需終端機）
- [ ] 窗口 1-3 秒內打開
- [ ] 顯示應用內容
- [ ] 按 F12 可打開開發者工具

---

## 🎨 第 3 階段: UI/資源測試

### 測試 3.1: 資源加載

在開發者工具的 Network 標籤檢查：

- [ ] **CSS 文件**
  - ✓ css/variables.css (應為 200 OK)
  - ✓ css/global.css
  - ✓ css/topbar.css
  - ❌ 無 404 錯誤

- [ ] **JavaScript 文件**
  - ✓ js/spa-router.js
  - ✓ js/topbar.js
  - ✓ js/pwa-register.js
  - ❌ 無語法錯誤

- [ ] **圖片資源**
  - ✓ assets/images/fuyu-logo.webp 顯示
  - ✓ assets/images/hero-forest.webp 顯示
  - ✓ favicon.ico 加載

### 測試 3.2: 樣式和布局

- [ ] 文字和圖片正確對齊
- [ ] 顏色和主題加載正確（深綠色主題）
- [ ] 響應式設計正常（調整窗口大小）
- [ ] 沒有明顯的排版錯誤

### 測試 3.3: 動畫和交互

- [ ] 首頁的螢火蟲粒子動畫正常
- [ ] 背景光球動畫順暢
- [ ] 滑鼠懸停效果正常
- [ ] 沒有卡頓或性能問題

---

## 🧭 第 4 階段: 導航測試

### 測試 4.1: SPA 路由

1. **首頁導航**
   ```
   點擊菜單 → 應該快速切換頁面（無刷新）
   ```
   - [ ] 點擊「品牌故事」
   - [ ] 點擊「建案資訊」
   - [ ] 點擊「3D 展示」
   - [ ] 點擊「地圖位置」
   - [ ] 返回首頁

**驗證：**
```javascript
// 在控制台檢查路由變化
window.addEventListener('popstate', () => console.log('✓ 路由改變'))

// 檢查 URL 變化
console.log('當前路徑:', window.location.pathname)
```

### 測試 4.2: 頁面功能

#### 📍 品牌頁面 (brand-*.html)
- [ ] 頁面加載正常
- [ ] 內容完整顯示
- [ ] 圖片正常加載
- [ ] 可以返回首頁

#### 🏢 建案資訊
- [ ] 列表顯示完整
- [ ] 可以點擊各項目
- [ ] 詳情頁面正常

#### 🌐 Google Map
- [ ] 地圖正常加載
- [ ] 可以點擊位置標記
- [ ] 可以縮放和拖動

#### 📸 全景/3D 展示
- [ ] 全景圖正常加載（如有）
- [ ] 3D 模型正確渲染（如有）
- [ ] 互動控制正常（滑鼠/鍵盤）

---

## 🔒 第 5 階段: PWA 功能測試

### 測試 5.1: Service Worker

在開發者工具 → Application → Service Workers：

- [ ] Service Worker 已註冊 (Status: activated)
- [ ] Scope 正確：http://localhost:3456

### 測試 5.2: 緩存

```javascript
// 在控制台檢查緩存
caches.keys().then(names => {
  console.log('✓ 快取版本:', names)
  names.forEach(name => {
    caches.open(name).then(cache => {
      cache.keys().then(requests => {
        console.log(name + ':', requests.length, '個文件')
      })
    })
  })
})
```

**應該看到：**
- ✓ fuyu-forest-1.0.x 快取
- ✓ > 10 個文件緩存

### 測試 5.3: 離線功能（可選）

1. 打開開發者工具 → Network → Offline 模式
2. 尋試刷新頁面
3. 應該仍能看到之前加載的內容

---

## 🌍 第 6 階段: 跨電腦測試

### 準備

1. 複製 `dist/富宇學森-1.0.0-portable.exe` 到 U 盤
2. 將 EXE 複製到另一台 Windows 電腦
3. 在該電腦上執行 EXE

### 測試項目

**在沒有 Node.js 的電腦上：**

- [ ] EXE 能直接運行（不需要安裝任何軟體）
- [ ] 應用正常啟動
- [ ] 所有功能正常
- [ ] 性能等同於開發電腦

**環境檢查：**
```powershell
# 檢查是否有 Node.js（應該沒有）
node --version
# 應該出現: 'node' is not recognized

# EXE 應該獨立運行
./富宇學森-1.0.0-portable.exe
```

---

## 🐛 第 7 階段: 問題排查

如果任何測試失敗，按以下順序排查：

### 步驟 1: 收集日誌
```bash
# 打開開發者工具 (F12)
# 複製所有紅色錯誤信息
# 特別關注:
# - 404 錯誤（找不到的文件）
# - CORS 錯誤（跨源問題）
# - 類型錯誤（JavaScript 錯誤）
```

### 步驟 2: 檢查資源路徑
```javascript
// 在控制台檢查每種資源類型
// CSS
fetch('css/variables.css').then(r => console.log('CSS:', r.status))

// JavaScript
fetch('js/spa-router.js').then(r => console.log('JS:', r.status))

// 圖片
fetch('assets/images/fuyu-logo.webp').then(r => console.log('Image:', r.status))
```

### 步驟 3: 檢查 Service Worker
```javascript
// 清除舊緩存
caches.keys().then(names => {
  Promise.all(names.map(name => caches.delete(name)))
    .then(() => console.log('✓ 緩存已清除'))
})

// 重新註冊 SW
navigator.serviceWorker.getRegistrations()
  .then(regs => regs.forEach(reg => reg.unregister()))
  .then(() => location.reload())
```

---

## ✅ 最終檢查清單

打包完成後，確保：

| 項目 | 狀態 |
|------|------|
| dist/ 文件夾存在 | ☐ |
| EXE 文件大小 > 150MB | ☐ |
| npm run electron-dev 正常 | ☐ |
| EXE 直接執行成功 | ☐ |
| 所有頁面加載正常 | ☐ |
| 無 404 或 JavaScript 錯誤 | ☐ |
| Service Worker 已註冊 | ☐ |
| 在另一台電腦可運行 | ☐ |
| 性能滿意（無卡頓） | ☐ |

**所有項目通過後，打包成功！** 🎉

---

## 📊 測試報告模板

```
測試日期: 2026-04-15
測試環境: Windows 11
EXE 版本: 1.0.0-portable

✅ 通過的測試:
- EXE 啟動
- 首頁加載
- 資源加載完整
- 無 JavaScript 錯誤

❌ 失敗的測試:
（如果有）

📝 備註:
（其他觀察事項）
```

祝測試順利！如有問題，請參考「除錯指南.md」
